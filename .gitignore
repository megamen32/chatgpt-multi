# Secrets & local artifacts — NEVER commit
cookie.json
*.json.gz
Trace-*.json
.DS_Store
**/.DS_Store
_metadata/
node_modules/
*.bak.*

# Local API dumps (may contain conversation data / tokens)
responce.json
response.json
